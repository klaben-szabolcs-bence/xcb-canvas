{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "by-gdb",
            "request": "launch",
            "name": "Launch(gdb)",
            "program": "build/${fileBasenameNoExtension}",
            "cwd": "${workspaceRoot}"
        }
    ]
}